#include <iostream>
#include <string>
#include <vector>

int main() {
    // ElevationDataset dataset {"all-tie-row1-2w-3h.dat", 3, 2};
    // std::cout << "width: " << dataset.Width() << " height: " << dataset.Height() << " MinEle: " << dataset.MinEle() << " MaxEle: " << dataset.MaxEle() << std::endl;
    // for (unsigned int i = 0; i < dataset.Height(); i++) {
    //     for (unsigned int j = 0; j < dataset.Width(); j++) {
    //         std::cout << dataset.DatumAt(i, j) << " " << std::end;
    //     }
    //     std::cout << "\n" << std::endl;
    // }
}
